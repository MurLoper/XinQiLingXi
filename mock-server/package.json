{
  "name": "xinqilingxi-mock-server",
  "version": "1.0.0",
  "description": "Standalone mock server for XinQiLingXi",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}